{
  "name": "@pirate/api-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "generate": "bun run scripts/generate-from-core.ts",
    "check:fresh": "bun run scripts/check-generated-fresh.ts",
    "check": "tsc --noEmit && bun run check:consumer",
    "check:consumer": "tsc --noEmit -p tsconfig.consumer.json"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^7.0.2"
  }
}
